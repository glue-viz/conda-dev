language: c

sudo: false

os:
    - osx
    - windows

env:
  global:
    - PYTHON_VERSION=3.7

before_install:
  - git clone git://github.com/astropy/ci-helpers.git
  - source ci-helpers/travis/setup_conda.sh
  - conda activate base

script:
  - ./build_all.sh
